import { CompetitionsForFleetPipe } from './competitions-for-fleet.pipe';

describe('CompetitionsForFleetPipe', () => {
  it('create an instance', () => {
    const pipe = new CompetitionsForFleetPipe();
    expect(pipe).toBeTruthy();
  });
});
